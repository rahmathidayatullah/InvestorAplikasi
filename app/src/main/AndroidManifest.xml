<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.edmt.investoraplikasi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".login.Login"
            android:label="SFSS INVESTOR"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InvestorMainActivity" />
        <activity android:name=".investor_mapslahan" />
        <activity android:name=".investor_belislot" />
        <activity android:name=".investor_tarikuang" />
        <activity android:name=".investor_verifikasi" />
        <activity android:name=".chat.MainActivity" />
        <activity android:name=".chat.ChatsFragment" />
        <activity android:name=".chat.FriendsFragment" />
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".chat.ProfileActivity" />
        <activity android:name=".chat.MessageAdapter" />
        <activity android:name=".chat.UsersActivity"></activity>
    </application>

</manifest>